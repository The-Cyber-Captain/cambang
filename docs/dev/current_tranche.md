# Current tranche

No active tranche.

The pre-Provider-brief hardening tranche (concurrency Blockers, provider
prompt/bounded contract enforcement, doc/hygiene sweep, and the scene 870
rig-capture admission-gate fix) was accepted; see `git log` for detail
rather than a retrospective account here. Maintainer is running deeper
manual tests before defining the next tranche (expected to be scoped
around compliance-verifier coverage and/or the Provider-creation brief
itself).

When the next tranche is defined, replace this file with its scope,
acceptance criteria, and near-term constraints -- do not accumulate a
history of completed work here (see `docs/dev/agent_context.md` §"Agent-
guidance maintenance").
