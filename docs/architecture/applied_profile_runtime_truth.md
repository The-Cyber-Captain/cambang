# Applied Capture Profile as Runtime Truth

Applied capture profile is part of runtime truth.
Snapshot exposes it directly.
`profile_version` is auxiliary change metadata.
Snapshot must be sufficient for runtime diagnosis.
Underlying config stores are not duplicated wholesale.
Only behaviourally relevant applied configuration is exposed.
